from setuptools import setup, find_packages

setup(
    name="ai_waiter",           # Name of your project
    version="0.1",
    packages=find_packages(),   # Automatically finds 'src' and other folders
)